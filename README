Plugin for custom form elements

Compatible
Windows, Mac, Ubuntu; IE6+, FF2+, Opera8+, Safari/Chrome (latest stable)

Parameters
Name                    Default value                                   Description
replaceRadios           false                                           replace <input type="radio" />
replaceCheckboxes       false                                           replace <input type="checkbox" />
replaceSelects          false                                           replace <select>
implementedClass        'customizedForm'                                customized form className
customizedClass         'customizedElement'                             customized element className
noCustomClass           'no-transform'                                  className of the element that shouldn't be custom
radioClass              'customRadio'                                   custom <input type="radio" /> className
radioClassChecked       'customRadioChecked'                            active ("checked") custom <input type="radio" />  className
checkboxClass           'customCheckbox'                                custom <input type="checkbox" /> className
checkboxClassChecked    'customCheckboxChecked'                         active ("checked") custom <input type="checkbox" /> className
selectOptionHTML        '<div><ul>{options}</ul></div>'</em>	        custom select dropdown's HTML
selectOptionItem        '<li><a href="#">{optionItem}</a></li>'         custom select dropdowns option elements HTML
selectClass             'customSelect'                                  custom <select> className
selectDisabledClass     'disabled-select'                               disabled custom <select> className
optionsClass            'customOptions'                                 custom <select> dropdown's className (visible)
optionsHiddenClass      'customOptionsHidden'                           custom <select> dropdown's className (hidden)
selectActiveClass       'customSelectActive'                            active (opened) custom select <select> className
selectHeight            false                                           custom <select> options top offset, false by default (is custom select's height)
selectImplemented       false                                           function after select customizin'
hideOnResize            true                                            hide custom select dropdowns on window resize
hideOnScroll            false                                           hide custom select dropdowns on window scroll